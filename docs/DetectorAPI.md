# Detector API

This document provides the technical specification for the Detector API.

## Interface
The Detector provides an interface to hook into the application execution flow.

## Key Functions
- `capture_state()`: Captures the current system state.
- `verify_transition()`: Checks if a transition between states is valid.
